apiVersion: v1
kind: Service
metadata:
  name: inventory-service
  namespace: shopnow
  labels:
    app: inventory-service
    part-of: shopnow
    version: "1.0"
spec:
  selector:
    app: inventory-service
  ports:
    - port: 8083
      targetPort: 8083