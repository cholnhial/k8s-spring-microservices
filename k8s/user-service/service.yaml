apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: shopnow
  labels:
    app: user-service
    part-of: shopnow
    version: "1.0"
spec:
  selector:
    app: user-service
  ports:
    - port: 8084
      targetPort: 8084
