apiVersion: v1
kind: Service
metadata:
  name: postgres-user
  namespace: shopnow
spec:
  clusterIP: None       # Headless — no virtual IP, DNS resolves to Pod IPs directly
  selector:
    app: postgres-user
  ports:
    - port: 5432
      targetPort: 5432