apiVersion: v1
kind: Service
metadata:
  name: postgres-order
  namespace: shopnow
spec:
  clusterIP: None       # Headless — no virtual IP, DNS resolves to Pod IPs directly
  selector:
    app: postgres-order
  ports:
    - port: 5432
      targetPort: 5432