apiVersion: v1
kind: ResourceQuota
metadata:
  name: shopnow-quota
  namespace: shopnow
spec:
  hard:
    requests.cpu: "8"
    requests.memory: "8Gi"
    limits.cpu: "10"
    limits.memory: "16Gi"
    pods: "26"